spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/conveyor_db
    username: postgres
    password: rasif1103!
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

springdoc:
  swagger-ui:
    path: /swagger-ui.html

server:
  port: 8080